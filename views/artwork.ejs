<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Artwork </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        .modal-body .card-img-top {
            max-height: 400px;
            max-width: 500px;
            height: auto;
        }
        .End {
            display: inline-block;
            margin-right: 10px;
        }
    </style>
</head>

<body>
    <%- include ('partials/navbar.ejs') %>
    <div class="container">
        <br>
        <div class="text-center">
            <h2>Artwork Details</h2>
        </div>
        <br>
        <% if (artworks) { %> <!-- Check if artwork data exists -->
            <div class="row">
                <div class="col-md-8">
                    <p><b>Artwork Name:</b><br>
                        <%= artworks.artwork_name %>
                    </p>
                    <p><b>Artist Name:</b><br>
                        <%= artworks.artist_name %>
                    </p>
                    <p><b>Year:</b><br>
                        <%= artworks.year %>
                    </p>
                    <p><b>Country:</b><br>
                        <%= artworks.country %>
                    </p>
                </div>
                <div class="col-md-4 text-end">
                        <a target="_blank" href="/images/<%= artworks.image %>">
                            <img src="/images/<%= artworks.image %>" class="img-fluid" alt="<%= artworks.artwork_name %>">
                        </a>
                    </p>
                </div>
            </div>
            <div class="card-footer">
                <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#myModal">
                    More Details
                </button>
            </div>

            <!-- MODAL here-->
            <div class="modal" id="myModal">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <!-- Header -->
                        <div class="modal-header">
                            <h3 class="modal-title">
                                <%= artworks.artwork_name %>
                            </h3>
                            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                        </div>
                        <!--  body -->
                        <div class="modal-body">
                            <a target="_blank" href="/images/<%= artworks.image %>">
                                <img class="card-img-top" src="/images/<%= artworks.image %>" alt="<%= artworks.artwork_name %> jpg">
                            </a>
                            <h3>Description</h3>
                            <p>
                                <%= artworks.dsc %>
                            </p>
                        </div>
                        <!--  footer -->
                        <div class="modal-footer">
                            <button type="button" class="btn btn-danger" data-bs-dismiss="modal">Close</button>
                        </div>
                    </div>
                </div>
            </div>
        <% } else { %>
            <p>No artwork found.</p>
        <% } %>

        <div class="End">
            <a href="/">Back</a><br>
            <!-- Edit Link -->
            <a href="/editgallery/<%= artworks.art_Id %>">Edit</a><br>
            <!-- Delete Link -->
            <a href="/deleteArtwork/<%= artworks.art_Id %>" onclick="return confirm('Are you sure you want to delete this Artwork?')">Delete</a>
        </div>
    </div>
</body>

</html>
